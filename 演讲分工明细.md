# 电影数据分析系统 — 演讲分工明细

**总时长：** 15 min 讲解 + 5 min Demo & Q&A
**顺序：** 按侧边栏视图顺序展开

---

## 第一位 — 项目介绍 / 问题背景 / 数据清洗（约 2.5 min）

### ① 项目背景
- 电影行业数据量庞大但分散，投资决策缺乏数据支撑
- 目标：构建一个可交互的电影数据分析系统，帮助分析票房规律、类型盈利、档期选择，并实现票房预测

### ② 数据来源
- 原始数据：TMDB 数据库，24 张 SQL 数据表，涵盖电影、评分、演员、制作公司等信息
- 最终数据集：45,346 部电影，其中含完整财务数据（budget / revenue）的有 5,375 部

### ③ Python 数据清洗流程（`scripts/build_dataset.py`）
- 无需本地数据库，直接解析 `.sql` 原始文件
- 多表 Join：movie + genres + rate + credit 等
- 关键处理步骤：
  - NULL 值过滤（budget / revenue 为 0 的记录剔除）
  - 多对多关系展开（一部电影多个类型 → `primary_genre`）
  - ROI 计算：`revenue / budget`
  - 评分转换：0.5–5 分制 → 0–10 分制（× 2）
  - 离群值处理：ROI 98th percentile 截断
- 输出：`data/processed_movies.csv`，包含 15+ 个分析字段

---

## 第二位 — 数据库设计 / ER 图 / SQL 导出（约 2.5 min）

### ① 数据库整体设计
- 基于 TMDB 数据设计的关系型数据库（MySQL / MariaDB）
- 共 11 张核心表，遵循第三范式设计

### ② 核心表说明（配合 ER 图）

| 表名 | 作用 |
|------|------|
| `movie` | 主表：title、budget、revenue、runtime、release_date |
| `genres` | 类型字典表 |
| `link_genres` | 电影-类型多对多关联表 |
| `rate` | 用户评分：user_id、tmdbid、rating、timestamp |
| `cast` | 演员信息：person_id、角色、出场顺序 |
| `credit` | 剧组信息：导演、编剧等 |
| `production_companies` | 制作公司 |
| `collection` | 系列电影（如漫威宇宙） |

### ③ 关系设计重点
- movie ↔ genres：多对多，通过 `link_genres` 连接
- movie ↔ rate：一对多（一部电影多个用户评分）
- movie ↔ cast / credit：一对多

### ④ SQL 文件导出
- 24 张表分别导出为 `.sql` 文件
- 项目中通过 Python 脚本直接解析，免去用户安装数据库的负担

---

## 第三位 — Overview / Revenue / Genres / Time（约 3 min）

### ① 系统整体界面
- 左侧固定筛选面板：年份范围、类型多选、快捷档期（5Y / 10Y / All）、最低评票数
- 顶部 6 个 KPI 卡片：总电影数、平均票房、平均 ROI、平均评分、总预算、总收入（含趋势对比）
- 右侧切换 8 个分析视图

### ② Overview 视图
- 所有图表综合展示，一屏全览系统核心分析

### ③ Revenue 视图
- 预算 vs 票房散点图（OLS 趋势线）：直观展示预算与票房正相关
- 预算 vs ROI 散点图：高预算 ≠ 高回报率，中小预算影片 ROI 波动更大

### ④ Genres 视图
- 类型盈利能力柱状图：支持切换"ROI"或"平均票房"排行
- 各类型评分分布箱线图：展示不同类型电影的口碑差异

### ⑤ Time 视图
- 月份与平均票房折线图（带填充）
- 结论：暑期档（6–8 月）和贺岁档（11–12 月）是票房峰值

---

## 第四位 — Chi-Square 卡方检验（约 2 min）

### ① 卡方检验原理（简要）
- 用于检验两个分类变量之间是否存在统计上的显著关联
- 原假设 H₀：两变量独立（无关联）；p < 0.05 时拒绝 H₀

### ② 系统中的分析维度
- **类型 vs 盈利性**：不同电影类型的盈利率是否存在显著差异？
- **预算等级 vs 盈利性**：Low / Mid / High / Blockbuster 四档预算与盈利的关联
- **上映档期 vs 收入**：Winter / Spring / Summer / Fall / Awards Season 与高票房的关系
- **评分等级 vs 盈利性**：Poor / Average / Good / Excellent 评分与盈利的关联

### ③ 派生字段设计
- `budget_tier`：4 档预算分级（< $10M / $10–50M / $50–150M / > $150M）
- `season`：月份映射为 5 个档期
- `profitable`：revenue > budget → Profitable / Not Profitable

### ④ 结论展示
- 展示 χ² 统计量、p 值、显著性结论
- 配合热力图（交叉表）可视化变量关联强度

---

## 第五位 — Regression 线性回归 / 机器学习预测（约 2.5 min）

### ① 线性回归分析
- 分析各特征对票房的线性影响：预算、评分、时长、上映月份
- 展示回归系数、R² 拟合优度
- 使用 `scikit-learn LinearRegression` + `statsmodels` 输出统计摘要

### ② 机器学习预测模块（Gradient Boosting）
- 训练数据：5,375 部有完整财务数据的电影
- 训练了 3 个模型：
  - **票房预测器**（对数变换回归，GradientBoostingRegressor）
  - **ROI 预测器**（97th percentile 截断，GradientBoostingRegressor）
  - **盈利概率分类器**（GradientBoostingClassifier，二分类）

### ③ 特征工程
- 输入特征：`log(budget)` / `genre`（OrdinalEncoder 编码）/ `release_month` / `runtime`
- 模型参数：300 棵树，学习率 0.05，最大深度 4，子采样 0.8

### ④ 预测输出
- 预测票房 + 置信区间（× 0.72 ~ × 1.42）
- 预测 ROI 倍数
- 盈利概率（0–100%）
- Top 5 可比较历史影片推荐（相近预算 × 相同类型）

---

## 第六位 — Insights / Export / 总结（约 2.5 min）

### ① Insights 视图（自动数据洞察）

**4 张 KPI 亮点卡片：**
- 最高 ROI 类型（Horror 等小成本类型领先）
- 最佳上映月份（暑期档平均票房最高）
- 盈利率（约 X% 的电影能收回成本）
- 长片 ROI（> 150 min 影片中位数 ROI 最高）

**4 张趋势图表：**
- 年代平均票房（1980s → 2010s 增长 2.4×）
- Top 5 类型 ROI 排行（横向柱状图）
- 评分区间 vs 平均票房（6–7 分段票房最高）
- 片长区间 vs ROI 折线图

**2 个关键发现：**
- **多类型策略**：超过半数电影横跨 2+ 类型，Comedy + Drama 和 Drama + Romance 最常见
- **评分甜点**：6–7 分段票房反超 8+ 高分影片，大众口味比叫好更赚钱

### ② Export 视图
- 支持将当前筛选结果一键导出为 CSV
- 用户可自定义年份、类型、最低评票数后下载数据子集

### ③ 总结
- **系统亮点：** 无需数据库、一键启动、8 个分析维度、ML 预测
- **局限性：** 预测置信区间较宽（± 28%–42%），仅基于 4 个特征；数据截至 TMDB 导出时间点
- **未来方向：** 接入实时 TMDB API、增加导演 / 演员等特征、加入更多统计模型

---

## Demo 环节（5 min）— 建议第三位或第六位主操作

| 时间 | 操作内容 |
|------|----------|
| 0–1 min | 打开系统，调整筛选（如：2010–2023，Action，最低票数 1000） |
| 1–2 min | 切换 Revenue / Genres 视图，指出关键图表结论 |
| 2–3.5 min | 进入 Regression 预测：输入案例参数，展示预测输出和可比影片 |
| 3.5–5 min | Q&A（其他成员辅助回答） |
